<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Página en reparación</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  <style>
    body {
      background-color: #f2f2f2;
    }
    .container {
      text-align: center;
      margin-top: 100px;
    }
    h1 {
      font-size: 3rem;
    }
    p {
      font-size: 1.5rem;
      margin-bottom: 30px;
    }
    #game-container {
      display: none;
    }
    .btn {

    }
  </style>
</head>

<body>
  <div class="container">
    <img src="https://i.imgur.com/aBQ1Ssx.jpeg" alt="Página en reparación">
    <h1>¡Lo sentimos!</h1>
    <p>Esta página se encuentra en reparación. Por favor, vuelve más tarde.</p>
    <a class="btn-primary" href="/">VOLVER</a>
    <p>Quieres volver</p>
   
    <button id="play-btn" class="btn btn-primary">Jugar a un juego mientras esperas</button>
    <div id="game-container">
      <h2>Adivina el número</h2>
      <p>Elige un número entre 1 y 10:</p>
      <input type="number" id="user-input" min="1" max="10">
      <button id="submit-btn" class="btn btn-primary">Comprobar</button>
      <p id="result"></p>
    </div>
  </div>
  
  <script>
    const playBtn = document.querySelector('#play-btn');
    const gameContainer = document.querySelector('#game-container');
    const userInput = document.querySelector('#user-input');
    const submitBtn = document.querySelector('#submit-btn');
    const result = document.querySelector('#result');
    
    playBtn.addEventListener('click', function() {
      playBtn.style.display = 'none';
      gameContainer.style.display = 'block';
    });
    
    submitBtn.addEventListener('click', function() {
      const randomNumber = Math.floor(Math.random() * 10) + 1;
      const userNumber = parseInt(userInput.value);
      
      if (isNaN(userNumber) || userNumber < 1 || userNumber > 10) {
        result.innerText = 'Elige un número válido entre 1 y 10.';
      } else if (userNumber === randomNumber) {
        result.innerText = '¡Has acertado! ¡Felicidades!';
      } else {
        result.innerText = `Lo siento, el número era ${randomNumber}. ¡Inténtalo de nuevo!`;
      }
      
      userInput.value = '';
    });
  </script>
</body>
</html>
